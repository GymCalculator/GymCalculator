1. What is the point of 1st step - secrets.yaml?
2. Check namespace everywhere and rename it to something meaningful! (SEARCH FOR "namespace: mynamespace")
3. Check docker image name everywhere! (SEARCH FOR "image: your-docker-image:tag")

4. In Kubernetes, namespaces provide a mechanism for isolating groups of resources within a single cluster.
   - s
